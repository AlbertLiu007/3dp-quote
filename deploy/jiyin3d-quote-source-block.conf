# Add these locations inside the HTTPS `server` block for
# `jiyin3d.com www.jiyin3d.com`, before the generic `location /` block.
#
# Do not add global extension blocks for JSON, JavaScript, TypeScript, or maps;
# the quote application requires normal browser assets under /quote/.

location = /quote-app {
    return 404;
}

location ^~ /quote-app/ {
    return 404;
}
